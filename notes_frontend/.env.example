# PUBLIC env vars are exposed to the client by Vite if prefixed with PUBLIC_
# Set this to the base URL of the notes backend service (notes_database)
PUBLIC_NOTES_API_BASE_URL=https://your-notes-backend.example.com
